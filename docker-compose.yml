services:
  postgres:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: claude_proxy
      POSTGRES_USER: claude
      POSTGRES_PASSWORD: claude
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
